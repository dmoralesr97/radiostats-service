spring:
  application:
    name: radiostats-service
  datasource:
    url: jdbc:h2:mem:radiostatsdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
